spring:
  application:
    name: configclient
  cloud:
    config:
      uri: http://localhost:8030
      enabled: true
  profiles:
    active: development



management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8042